diff --git a/config.def.h b/config.def.h
index 11601be..484c5f8 100644
--- a/config.def.h
+++ b/config.def.h
@@ -4,6 +4,7 @@
                         ((hex >> 8) & 0xFF) / 255.0f, \
                         (hex & 0xFF) / 255.0f }
 /* appearance */
+static const char* startup_script	   = "";
 static const int sloppyfocus               = 1;  /* focus follows mouse */
 static const int bypass_surface_visibility = 0;  /* 1 means idle inhibitors will disable idle tracking even if it's surface isn't visible  */
 static const int enablegaps                = 1;  /* 1 means gaps are enabled */
diff --git a/dwl.c b/dwl.c
index 23dc95c..1d6c4f4 100644
--- a/dwl.c
+++ b/dwl.c
@@ -339,7 +339,7 @@ static void requestdecorationmode(struct wl_listener *listener, void *data);
 static void requeststartdrag(struct wl_listener *listener, void *data);
 static void requestmonstate(struct wl_listener *listener, void *data);
 static void resize(Client *c, struct wlr_box geo, int interact);
-static void run(char *startup_cmd);
+static void run(char *input_startup_cmd);
 static void setcursor(struct wl_listener *listener, void *data);
 static void setcursorshape(struct wl_listener *listener, void *data);
 static void setfloating(Client *c, int floating);
@@ -2452,7 +2452,7 @@ resize(Client *c, struct wlr_box geo, int interact)
 }
 
 void
-run(char *startup_cmd)
+run(char *input_startup_cmd)
 {
 	/* Add a Unix socket to the Wayland display. */
 	const char *socket = wl_display_add_socket_auto(dpy);
@@ -2466,7 +2466,25 @@ run(char *startup_cmd)
 		die("startup: backend_start");
 
 	/* Now that the socket exists and the backend is started, run the startup command */
-	if (startup_cmd) {
+	if (input_startup_cmd) {
+		int piperw[2];
+		if (pipe(piperw) < 0)
+			die("input startup: pipe:");
+		if ((child_pid = fork()) < 0)
+			die("input startup: fork:");
+		if (child_pid == 0) {
+			setsid();
+			dup2(piperw[0], STDIN_FILENO);
+			close(piperw[0]);
+			close(piperw[1]);
+			execl("/bin/sh", "/bin/sh", "-c", input_startup_cmd, NULL);
+			die("input startup: execl:");
+		}
+		dup2(piperw[1], STDOUT_FILENO);
+		close(piperw[1]);
+		close(piperw[0]);
+	}
+	if (startup_script){
 		int piperw[2];
 		if (pipe(piperw) < 0)
 			die("startup: pipe:");
@@ -2477,13 +2495,13 @@ run(char *startup_cmd)
 			dup2(piperw[0], STDIN_FILENO);
 			close(piperw[0]);
 			close(piperw[1]);
-			execl("/bin/sh", "/bin/sh", "-c", startup_cmd, NULL);
+			execl("/bin/sh", "/bin/sh", "-c", startup_script, NULL);
 			die("startup: execl:");
 		}
 		dup2(piperw[1], STDOUT_FILENO);
 		close(piperw[1]);
 		close(piperw[0]);
-	}
+	};
 
 	/* Mark stdout as non-blocking to avoid people who does not close stdin
 	 * nor consumes it in their startup script getting dwl frozen */
@@ -3467,12 +3485,12 @@ xwaylandready(struct wl_listener *listener, void *data)
 int
 main(int argc, char *argv[])
 {
-	char *startup_cmd = NULL;
+	char *input_startup_cmd = NULL;
 	int c;
 
 	while ((c = getopt(argc, argv, "s:hdv")) != -1) {
 		if (c == 's')
-			startup_cmd = optarg;
+			input_startup_cmd = optarg;
 		else if (c == 'd')
 			log_level = WLR_DEBUG;
 		else if (c == 'v')
@@ -3487,7 +3505,7 @@ main(int argc, char *argv[])
 	if (!getenv("XDG_RUNTIME_DIR"))
 		die("XDG_RUNTIME_DIR must be set");
 	setup();
-	run(startup_cmd);
+	run(input_startup_cmd);
 	cleanup();
 	return EXIT_SUCCESS;
 
